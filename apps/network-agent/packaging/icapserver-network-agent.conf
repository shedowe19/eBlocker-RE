# Grant only the server process access to the local observation and explicitly
# activated control sockets. SO_PEERCRED additionally verifies icapd UID/GID.
# A server restart is required after installation to obtain this group.
[Service]
SupplementaryGroups=eblocker-network-agent eblocker-network-control
